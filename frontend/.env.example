# Lambda Function URL emitted by `terraform -chdir=infra apply` (output: contact_function_url).
# Leave blank for local dev that doesn't need DynamoDB persistence — the WhatsApp redirect still works.
VITE_CONTACT_API=
